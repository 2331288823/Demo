package com.example.star.aiwork.domain.model

/**
 * A lightweight data class representing only the title and ID of a session.
 * Used for efficiently populating session lists in the UI.
 */
data class SessionTitle(val id: String, val name: String)
